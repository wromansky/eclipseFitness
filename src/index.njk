---
layout: base
permalink: index.html
eleventyNavigation:
    key: "Home"
style: {
    inline: "
        #page-hero {
            --height: 320px;
            padding-block-start: clamp(3rem, 6vw, 6rem);
            padding-block-end: clamp(3rem, 6vw, 6rem);
            font-size: clamp(1rem, 1.5vw, 1.25rem);
        }

        #page-hero .title {
            --h-font-size: clamp(2.5rem, 5vw, 5rem);
            --h-font-weight: 600;
        }

        #page-hero .preview {
            border-radius: 2rem;
            overflow: hidden;
        }
    "
}
---

<section id="page-hero" class="mb-12" style="--bg: var(--color-theme-fade)">
    <div class="container flex flex-col md-flex-row justify-center md-justify-between items-center">
        <div>
            <h1 class="title">{{ meta.siteName }}</h1>
            <div class="slogan">{{ meta.siteDescription }}</div>
            {% include "theme-switcher.njk"%}
        </div>
        <div class="preview mt-8 md-mt-0 md-ms-4 xl-ms-8">
            {% image "https://picsum.photos/id/7/840/840", "Beatiful picture", { lazy: false } %}
        </div>
    </div>
</section>

<div class="container my-12">
{% renderTemplate "md" %}
Welcome to Eclipse Fitness!

Are you ready to transform your health and fitness? Look no further! At Eclipse Fitness, we're dedicated to helping you achieve your fitness goals with personalized coaching and expert guidance. Whether you're looking to lose weight, build muscle, or improve your overall well-being, our team of experienced coaches is here to support you every step of the way. Let's embark on this journey together and unlock your full potential. Welcome to a healthier, happier you!

## Services

{% endrenderTemplate %}
</div>
